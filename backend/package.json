{
  "name": "neon-planner-backend",
  "version": "1.0.0",
  "description": "API REST para o Gerenciador de Tarefas Neon Planner",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "migrate": "npx knex migrate:latest --knexfile ./knexfile.js",
    "migrate:prod": "NODE_ENV=production npx knex migrate:latest --knexfile ./knexfile.js --env production"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "pg": "^8.11.0",
    "knex": "^2.5.1"
  }
}